---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "OS Release Ex: windows10.yaml, windows7.yaml "
    path: "%{facts.os.family}%{facts.operatingsystemmajrelease}.yaml"
  - name: "Common hierarchy levels"
    path:  "common.yaml"
